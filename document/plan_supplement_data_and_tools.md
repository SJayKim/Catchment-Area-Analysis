# 상권분석 AI Agent — 보강 문서: 데이터·LightRAG·프레임워크 상세

> **원본 계획서:** `commercial_district_agent_plan.md`
> **작성일:** 2026-03-19
> **목적:** 기존 계획서의 4개 영역을 실행 수준으로 구체화

---

## 1. 에이전트별 데이터 소스 상세 매핑

기존 계획서에서는 "공공데이터 MCP", "지도 MCP" 등 추상적으로 기술했으나, 실제 구현을 위해 **각 에이전트가 호출해야 할 구체적 API와 데이터셋 ID**를 정리합니다.

### 1-1. 인구분석 Agent

| 데이터 | 소스 | API/데이터셋 ID | 갱신 주기 | 비고 |
|--------|------|-----------------|----------|------|
| 유동인구 (시간대/요일/성별/연령) | 소상공인진흥공단 상권정보 | `data.go.kr #15012005` (OpenAPI) | 분기별 | 상권코드 기반 조회, 핵심 데이터 |
| 서울 생활인구 | 서울 열린데이터광장 | `data.seoul.go.kr` 서울생활인구 (내국인/외국인) | 월별 | 서울 한정, 행정동 단위 시간대별 인구 |
| 주민등록 거주인구 | 통계청 KOSIS | `kosis.kr` 주민등록인구통계 | 월별 | 행정동 단위 연령/성별 |
| 직장인구 (사업체종사자) | 통계청 | `data.go.kr #15083827` 전국사업체조사 | 연별 | 행정동×업종별 종사자 수 |
| 아파트 세대수 | 국토교통부 | 공동주택단지 목록 API | 수시 | 반경 내 거주 수요 추정용 |

**현재 계획서의 Gap:**
- 서울 외 지역의 유동인구 데이터가 부족함 → **KT/SKT 통신 기반 유동인구 데이터** 유료 구매를 Phase 2에서 검토 필요
- 소상공인진흥공단 데이터는 분기 지연이 있어 **실시간성이 낮음** → 카카오/네이버 플레이스의 "현재 혼잡도" 데이터로 보완 가능

---

### 1-2. 매출분석 Agent

| 데이터 | 소스 | API/데이터셋 ID | 갱신 주기 | 비고 |
|--------|------|-----------------|----------|------|
| 상권 내 업종별 매출 | 소상공인진흥공단 상권정보 | `data.go.kr #15012005` (추정매출 항목) | 분기별 | 카드사 빅데이터 기반 추정치 |
| 서울 카드매출 통계 | 서울 열린데이터광장 | 서울시 우리마을가게 상권분석서비스 | 분기별 | 서울 한정, 행정동×업종 |
| 프랜차이즈 평균 매출 | 공정거래위원회 | 가맹사업정보제공시스템 (franchise.ftc.go.kr) | 연별 | 정보공개서 기반 브랜드별 평균 매출 |
| 업종별 평균 객단가 | 한국외식산업연구원 / 자체 구축 | 없음 (수동 수집) | - | 크롤링 또는 리서치 기반 DB 자체 구축 |
| 배달앱 매출 트렌드 | 없음 (공공API 미제공) | - | - | 배달의민족/요기요 공식 데이터 없음 → 뉴스/리포트 기반 추정 |

**현재 계획서의 Gap:**
- **객단가 데이터가 공공 API로 없음** → 업종별 평균 객단가 테이블을 자체 구축해야 함 (프랜차이즈 정보공개서 + 카드매출/거래건수 역산)
- **배달 매출 비중**이 외식업에서 30~50%인데 공공데이터가 없음 → LightRAG에 뉴스/리포트를 적재하여 업종별 배달 비중을 Knowledge로 관리

---

### 1-3. 경쟁분석 Agent

| 데이터 | 소스 | API/데이터셋 ID | 갱신 주기 | 비고 |
|--------|------|-----------------|----------|------|
| 반경 내 점포 목록 | 소상공인진흥공단 상가정보 | `data.go.kr #15012005` (상가업소 조회) | 분기별 | 반경/업종별 상가 검색 가능 |
| 네이버 플레이스 업체 정보 | 네이버 지도 API | Naver Search API (지역 검색) | 실시간 | 리뷰수, 평점, 영업시간 |
| 카카오 플레이스 업체 정보 | 카카오 로컬 API | `dapi.kakao.com/v2/local/search/keyword` | 실시간 | 카테고리별 주변 검색 |
| 개업/폐업 현황 | 국세청 | `data.go.kr #15081808` 사업자등록상태조회 | 실시간 | 특정 사업자번호의 상태 확인 |
| 상권 내 개폐업률 | 소상공인진흥공단 | `data.go.kr #15012005` (점포 증감) | 분기별 | 상권 단위 업종별 개폐업 수 |
| 구글 맵 리뷰/평점 | Google Places API | Places API (Nearby Search) | 실시간 | 외국인 타겟 상권에 유용 |

**현재 계획서의 Gap:**
- 네이버/카카오 플레이스 API는 **리뷰 본문까지 공식 제공하지 않음** → 크롤링이 필요하나 ToS 위반 가능성 → 대안: 네이버 블로그 검색 API로 "장소명+리뷰" 검색하여 간접 수집
- **경쟁업체의 매출 추정**은 불가능 → 점포 수 + 상권 전체 매출로 "점포당 평균 매출" 역산 방식 사용

---

### 1-4. 입지분석 Agent

| 데이터 | 소스 | API/데이터셋 ID | 갱신 주기 | 비고 |
|--------|------|-----------------|----------|------|
| 지하철 승하차 인원 | 서울교통공사 | `data.seoul.go.kr` 지하철 승하차 | 월별 | 역별/시간대별 |
| 버스 승하차 인원 | 서울시 | `data.seoul.go.kr` 버스 승하차 | 월별 | 정류장별 |
| 주변 POI (학교, 병원, 관공서 등) | 카카오 로컬 API | `dapi.kakao.com/v2/local/search/category` | 실시간 | 카테고리 코드로 검색 |
| 주차장 정보 | 공영주차장 API | `data.go.kr #15012896` | 수시 | 주변 주차 가능 여부 |
| 도보 거리/시간 | 카카오모빌리티 / Naver Direction | 도보 경로 API | 실시간 | 역→매장 도보 시간 |
| 집객 시설 (대형마트, 백화점 등) | 카카오 로컬 API | 카테고리 검색 | 실시간 | 앵커 테넌트 파악 |

**현재 계획서의 Gap:**
- **가시성(Visibility) 분석**을 로드뷰로 한다고 했으나, 네이버/카카오 로드뷰 API는 이미지를 직접 제공하지 않음 → Vision LLM 연동은 Phase 3 이후로 미루고, MVP에서는 **"1층 여부 + 대로변 여부 + 코너 여부"** 같은 rule-based 가시성 점수 사용
- **교통 데이터는 서울 중심** → 서울 외 지역은 TAGO (전국대중교통정보) API `data.go.kr #15098529` 활용

---

### 1-5. 트렌드분석 Agent

| 데이터 | 소스 | API/데이터셋 ID | 갱신 주기 | 비고 |
|--------|------|-----------------|----------|------|
| 검색 트렌드 | 네이버 데이터랩 | `datalab.naver.com` 검색어 트렌드 API | 실시간 | 업종 키워드 검색량 추이 |
| 블로그 리뷰 | 네이버 검색 API | `openapi.naver.com` 블로그 검색 | 실시간 | 지역+업종 키워드 검색 |
| 뉴스 | 네이버 검색 API | `openapi.naver.com` 뉴스 검색 | 실시간 | 상권/업종 관련 뉴스 |
| 인스타그램 해시태그 | 비공식 (크롤링 필요) | 없음 | - | 공식 API 폐쇄됨 → 대안 필요 |
| 소비자 트렌드 리포트 | 한국소비자원, 각 리서치사 | 수동 수집 | 분기~연별 | LightRAG에 적재하여 Knowledge화 |

**현재 계획서의 Gap:**
- **인스타그램 해시태그 API가 사실상 없음** → 대안: (1) 네이버 블로그/카페 검색량으로 대체, (2) Google Trends API (pytrends), (3) 자체 크롤러 (법적 리스크 있음)
- 트렌드는 **정량 데이터보다 정성 데이터** 비중이 높음 → LightRAG에 리포트/뉴스를 적재하고 에이전트가 RAG로 검색하는 구조가 핵심

---

### 1-6. 재무분석 Agent

| 데이터 | 소스 | API/데이터셋 ID | 갱신 주기 | 비고 |
|--------|------|-----------------|----------|------|
| 상가 임대 시세 | 한국부동산원 | 상업용부동산 임대동향조사 | 분기별 | 지역별 층별 임대료 |
| 상가 실거래가 | 국토교통부 | `data.go.kr #15126463` 상업업무용 매매 실거래가 | 월별 | 법정동코드+계약년월로 조회 |
| 창업 지원 대출 | 기업마당 (bizinfo.go.kr) | 지원사업정보 API | 수시 | 업종/지역별 지원사업 목록 |
| 창업 지원금 | K-Startup (창업진흥원) | `data.go.kr #15125364` K-Startup 조회서비스 | 수시 | 사업공고 정보 |
| 정책자금 실적 | 중소벤처기업진흥공단 | `data.go.kr #15069962` 업종별 지원 현황 (파일) | 연별 | 업종별 대출 승인 패턴 |
| 인테리어/설비 비용 | 자체 DB | 없음 (수동 구축) | - | 업종별 평당 인테리어 비용 테이블 |
| 인건비 기준 | 최저임금위원회 | 최저임금 고시 | 연별 | 인건비 산출 기준 |
| 4대보험 요율 | 국민건강보험공단 등 | 공시 자료 | 연별 | 고용주 부담 비용 계산 |

**현재 계획서의 Gap:**
- **권리금 데이터는 공공 API가 없음** → 부동산114, 점포라인 등 민간 플랫폼 크롤링 또는 유사 거래 기반 자체 추정 모델 필요
- **업종별 초기 투자비 (인테리어, 설비)** 공공 데이터 없음 → 프랜차이즈 정보공개서의 "가맹점 평균 투자비"를 기반으로 자체 DB 구축

---

### 1-7. 리스크분석 Agent

| 데이터 | 소스 | 용도 |
|--------|------|------|
| 상권 개폐업률 | 소상공인진흥공단 (위 경쟁분석과 공유) | 업종 생존율 |
| 재개발/재건축 구역 | 서울시 도시계획포털, 각 지자체 | 상권 소멸 리스크 |
| GTX/지하철 연장 계획 | 국토교통부 보도자료 | 상권 변동 예측 |
| 인구 감소 추세 | 통계청 장래인구추계 | 장기 구조적 리스크 |
| 법률 변경 예고 | 국가법령정보센터 (law.go.kr) | 규제 리스크 |
| 모든 선행 에이전트 결과 | 내부 (State 전달) | 종합 리스크 산정 |

---

### 1-8. 부동산분석 Agent

| 데이터 | 소스 | API/데이터셋 ID | 비고 |
|--------|------|-----------------|------|
| 상가 매매 실거래가 | 국토교통부 | `data.go.kr #15126463` | 법정동코드 5자리 + 년월 |
| 상가 임대차 실거래가 | 국토교통부 | `data.go.kr #15126471` (확인 필요) | 보증금/월세 |
| 공실률 | 한국부동산원 | 상업용부동산 임대동향조사 (파일) | 분기별, 권역별 |
| 건축물대장 | 국토교통부 | `data.go.kr #15044713` 건축물대장 API | 용도, 면적, 층수 |
| 토지이용계획 | 국토교통부 | 토지이용규제정보서비스 (luris.go.kr) | 용도지역/지구 |
| 현재 임대 매물 | 민간 (네이버부동산, 직방 등) | 공식 API 없음 | 크롤링 또는 수동 |

---

### 1-9. 규제/법률분석 Agent

| 데이터 | 소스 | API/데이터셋 ID | 비고 |
|--------|------|-----------------|------|
| 법령 검색 | 국가법령정보센터 | `law.go.kr` 법령 API | 식품위생법, 건축법 등 |
| 지자체 조례 | 자치법규정보시스템 | `elis.go.kr` | 지역별 영업시간 제한 등 |
| 인허가 요건 | 정부24 민원 안내 | `gov.kr` (구조화 안 됨) | 자체 DB로 구축 필요 |
| 위생 기준 | 식품의약품안전처 | 식품안전나라 (foodsafetykorea.go.kr) | 업종별 위생 기준 |

---

### 1-10. 데이터 소스 총괄 — 가용성 매트릭스

| 데이터 유형 | API 제공 | 파일 제공 | 자체 구축 필요 | 우선순위 |
|-------------|---------|----------|---------------|----------|
| 유동인구 | O (소진공) | O | - | P0 (MVP 필수) |
| 상가 점포 목록 | O (소진공) | O | - | P0 |
| 업종별 추정매출 | O (소진공) | O | - | P0 |
| 지도/POI/경로 | O (카카오/네이버) | - | - | P0 |
| 상가 실거래가 | O (국토부) | O | - | P1 |
| 건축물대장 | O (국토부) | - | - | P1 |
| 대중교통 승하차 | O (서울시/TAGO) | O | - | P1 |
| 뉴스/블로그 | O (네이버 검색) | - | - | P1 |
| 검색 트렌드 | O (네이버 데이터랩) | - | - | P1 |
| 법령/조례 | O (law.go.kr) | - | - | P2 |
| 창업 지원사업 | O (기업마당) | - | - | P2 |
| **권리금** | X | X | **O (크롤링/추정)** | P2 |
| **업종별 객단가** | X | X | **O (자체 DB)** | P1 |
| **업종별 초기투자비** | X | X | **O (정보공개서 기반)** | P1 |
| **인테리어 비용** | X | X | **O (시장 조사)** | P2 |
| **인스타그램 트렌드** | X | X | **O (대체 소스)** | P3 |
| **배달 매출 비중** | X | X | **O (뉴스/리포트)** | P2 |

> **핵심 발견:** 공공 API로 60~70%의 데이터를 커버할 수 있으나, **재무분석의 핵심 데이터(권리금, 객단가, 초기투자비)**는 자체 구축이 불가피합니다.

---

## 2. 데이터 구축 방법

### 2-1. 공공 API 데이터 (자동 수집)

```
수집 파이프라인:

[APScheduler — 주기적 실행]
        ↓
┌────────────────────────────────────────────────────────┐
│  Collector Layer                                        │
│                                                        │
│  ┌─────────────────────────────────────────────────┐   │
│  │  semas_collector.py (소상공인진흥공단)             │   │
│  │  ─────────────────────────────────────           │   │
│  │  1. data.go.kr API 키로 인증                      │   │
│  │  2. 전국 상권코드 목록 순회                        │   │
│  │  3. 상권별 유동인구/매출/점포 데이터 수집           │   │
│  │  4. JSON → PostgreSQL (정형) + LightRAG (텍스트)   │   │
│  │                                                    │   │
│  │  실행 주기: 분기 1회 (데이터 갱신 시점 맞춤)        │   │
│  │  예상 소요: 전국 ~3,000 상권 × 4 API = ~12,000 호출│   │
│  │  Rate Limit: 일 1,000건 → 약 12일 분산 수집        │   │
│  └─────────────────────────────────────────────────┘   │
│                                                        │
│  ┌─────────────────────────────────────────────────┐   │
│  │  molit_collector.py (국토교통부)                   │   │
│  │  ─────────────────────────────────────           │   │
│  │  1. 법정동코드 5자리 목록 순회                     │   │
│  │  2. 상업용 매매/임대 실거래가 수집                  │   │
│  │  3. 건축물대장 주요 필드 수집                      │   │
│  │                                                    │   │
│  │  실행 주기: 월 1회                                 │   │
│  └─────────────────────────────────────────────────┘   │
│                                                        │
│  ┌─────────────────────────────────────────────────┐   │
│  │  seoul_collector.py (서울 열린데이터)              │   │
│  │  ─────────────────────────────────────           │   │
│  │  1. 생활인구 데이터 (행정동×시간대×성별×연령)      │   │
│  │  2. 지하철/버스 승하차 데이터                      │   │
│  │  3. 카드매출 통계 (우리마을가게 상권분석)           │   │
│  │                                                    │   │
│  │  실행 주기: 월 1회                                 │   │
│  └─────────────────────────────────────────────────┘   │
└────────────────────────────────────────────────────────┘
        ↓
┌────────────────────────────────────────────────────────┐
│  Preprocessor Layer                                     │
│                                                        │
│  1. 좌표계 통일: 모든 좌표를 WGS84 (EPSG:4326)로 변환  │
│  2. 코드 매핑: 행정동코드 ↔ 법정동코드 ↔ 상권코드 연결  │
│     → mapping_table.json 자체 구축 (1회성)              │
│  3. 결측값 처리:                                        │
│     - 유동인구 0인 시간대 → 인접 시간대 보간            │
│     - 매출 데이터 없는 업종 → null 표시 (추정 안 함)     │
│  4. 이상치 탐지:                                        │
│     - 매출이 전분기 대비 300% 이상 변동 → 플래그         │
│     - 유동인구 급변 → 재개발/이벤트 여부 확인            │
│  5. 시계열 정렬: 분기별 데이터를 시간순 정렬 & 인덱싱    │
└────────────────────────────────────────────────────────┘
        ↓
┌────────────────────────────────────────────────────────┐
│  Storage Layer                                          │
│                                                        │
│  PostgreSQL + PostGIS:                                  │
│  ├── districts (상권 경계 폴리곤, 메타데이터)            │
│  ├── population_stats (상권×분기별 인구 통계)            │
│  ├── revenue_stats (상권×업종×분기별 매출)               │
│  ├── stores (개별 점포 정보, 좌표)                       │
│  ├── real_estate_transactions (실거래가)                 │
│  └── transit_stats (역/정류장별 승하차)                   │
│                                                        │
│  LightRAG Knowledge Graph:                              │
│  ├── 상권별 특성 요약 텍스트                             │
│  ├── 뉴스/리포트 (트렌드 분석용)                         │
│  ├── 법령/조례 텍스트                                    │
│  └── 과거 분석 리포트 (학습용)                           │
│                                                        │
│  Redis:                                                 │
│  ├── 실시간 API 응답 캐시 (TTL: 24h)                    │
│  └── 카카오/네이버 지도 API 응답 캐시 (TTL: 7d)         │
└────────────────────────────────────────────────────────┘
```

### 2-2. 자체 구축 데이터 (수동 + 반자동)

#### A. 업종별 객단가 테이블

```
구축 방법:
1. 프랜차이즈 정보공개서에서 "매출액 ÷ 거래건수" 역산
   → franchise.ftc.go.kr에서 주요 50개 업종의 대표 브랜드 수집
2. 소진공 추정매출 ÷ 카드 결제 건수로 검증
3. 업종 대분류(한식, 카페, 미용 등) × 상권 등급(A/B/C)별 객단가 매트릭스

저장: PostgreSQL 테이블 + LightRAG (에이전트 프롬프트 컨텍스트용)

예시 데이터:
┌──────────────┬──────────┬──────────┬──────────┐
│ 업종          │ A급 상권  │ B급 상권  │ C급 상권  │
├──────────────┼──────────┼──────────┼──────────┤
│ 카페          │ 7,500원  │ 6,000원  │ 5,000원  │
│ 한식 (일반)   │ 12,000원 │ 9,500원  │ 8,000원  │
│ 퓨전한식      │ 18,000원 │ 14,000원 │ 11,000원 │
│ 디저트/베이커리│ 8,500원  │ 6,500원  │ 5,500원  │
│ 미용실        │ 25,000원 │ 18,000원 │ 15,000원 │
└──────────────┴──────────┴──────────┴──────────┘
```

#### B. 업종별 초기 투자비 테이블

```
구축 방법:
1. 프랜차이즈 정보공개서의 "가맹점 예상 투자비" 항목 수집
   → 인테리어, 설비, 보증금, 기타 항목별 분리
2. 비프랜차이즈는 업계 평균으로 추정 (리서치 기반)
3. 면적별 단가로 정규화 (원/평)

저장: PostgreSQL + LightRAG

예시 데이터:
┌──────────────┬────────────┬────────────┬───────────┐
│ 업종          │ 인테리어/평 │ 설비/평     │ 기타 고정  │
├──────────────┼────────────┼────────────┼───────────┤
│ 카페 (20평)   │ 200~350만  │ 100~200만  │ 500~800만 │
│ 한식 (25평)   │ 150~250만  │ 150~300만  │ 300~500만 │
│ 미용실 (15평) │ 250~400만  │ 200~400만  │ 200~300만 │
└──────────────┴────────────┴────────────┴───────────┘
```

#### C. 상권별 권리금 추정 모델

```
구축 방법:
1. 학습 데이터: 소상공인진흥공단 상가 정보의 "권리금 있음/없음" 필드
   + 부동산 커뮤니티/중개사이트의 권리금 시세 (크롤링)
2. 추정 모델 (간단한 회귀):
   권리금 ≈ f(업종, 상권등급, 월매출, 유동인구, 임대료, 영업기간)
3. 초기에는 rule-based → 데이터 축적 후 ML 모델로 전환

저장: 모델 파라미터 + PostgreSQL
```

### 2-3. 실시간 데이터 (On-demand 호출)

```
에이전트 실행 시 실시간 호출하는 데이터:

┌──────────────────────────┬───────────────────────────────┐
│ 데이터                    │ 호출 방식                      │
├──────────────────────────┼───────────────────────────────┤
│ 카카오 로컬 검색 (POI)    │ MCP → 카카오 REST API         │
│ 네이버 검색 (뉴스/블로그) │ MCP → 네이버 검색 API         │
│ 네이버 데이터랩 (트렌드)  │ MCP → 네이버 데이터랩 API     │
│ 지오코딩 (주소→좌표)      │ MCP → 카카오 지오코딩 API     │
│ 도보 경로                 │ MCP → 카카오 모빌리티 API     │
│ 법령 검색                 │ MCP → law.go.kr API          │
│ 창업 지원사업 검색        │ MCP → 기업마당 API            │
└──────────────────────────┴───────────────────────────────┘

캐싱 전략:
- 지오코딩 결과: Redis TTL 30일 (주소는 잘 안 바뀜)
- POI 검색 결과: Redis TTL 7일 (점포 변동 고려)
- 뉴스 검색: Redis TTL 24시간
- 트렌드: Redis TTL 24시간
- 법령: Redis TTL 30일
```

### 2-4. data.go.kr API 키 발급 & 관리

```
필요한 API 키 목록 (Phase별):

Phase 0 (사전 준비):
├── data.go.kr 통합 인증키 (1개로 여러 API 사용 가능)
├── 카카오 REST API 키 (developers.kakao.com)
├── 네이버 검색/데이터랩 API 키 (developers.naver.com)
└── LLM API 키 (Anthropic, Google AI)

Phase 1:
├── 소상공인진흥공단 상가정보 API 활용 신청
├── 카카오 로컬 API 사용량 확인 (일 30만건 무료)
└── 네이버 검색 API 사용량 확인 (일 25,000건 무료)

Phase 2:
├── 국토교통부 실거래가 API 활용 신청
├── 건축물대장 API 활용 신청
├── 기업마당 지원사업 API 활용 신청
├── K-Startup 조회서비스 API 활용 신청
└── 국가법령정보 API 활용 신청

관리 방식:
- .env 파일에 통합 관리
- Docker Compose의 environment로 컨테이너에 주입
- API 키 갱신 알림 (대부분 2년 유효)
```

---

## 3. LightRAG 활용법 상세

### 3-1. LightRAG가 이 프로젝트에서 해결하는 문제

```
문제: 에이전트들이 분석할 때 "정형 데이터(숫자)"만으로는 부족한 컨텍스트가 있음

예시:
- "강남역 상권은 최근 젠트리피케이션 우려가 있다" → 뉴스에서만 나오는 정보
- "퓨전한식은 2025년 하반기부터 성장세가 둔화되고 있다" → 트렌드 리포트
- "식품위생법 제44조에 따르면 영업장 면적이 150㎡ 이상이면..." → 법령 텍스트
- "강남역 이면도로 매출은 대로변 대비 40~60% 수준" → 과거 분석 교훈

이런 비정형 지식을 LightRAG의 Knowledge Graph로 구조화하여
에이전트가 분석 시 자동으로 관련 컨텍스트를 검색하게 함
```

### 3-2. LightRAG 초기화 & 설정

```python
# backend/memory/knowledge_graph.py

import asyncio
from lightrag import LightRAG, QueryParam
from lightrag.llm.openai import openai_compatible_complete, openai_compatible_embed
from lightrag.kg.shared_storage import initialize_pipeline_status
from lightrag.utils import setup_logger

setup_logger("lightrag", level="INFO")

async def create_rag_instance() -> LightRAG:
    """상권분석 전용 LightRAG 인스턴스 생성"""
    rag = LightRAG(
        working_dir="./data/lightrag_store",

        # LLM 설정 — Gemini 2.5 Flash (비용 효율적)
        llm_model_func=openai_compatible_complete,
        llm_model_name="gemini-2.5-flash",
        llm_model_kwargs={
            "api_base": "https://generativelanguage.googleapis.com/v1beta/openai",
            "api_key": GEMINI_API_KEY,
        },

        # 임베딩 설정 — bge-m3 (한국어 품질)
        embedding_func=openai_compatible_embed,
        embedding_model_name="BAAI/bge-m3",
        embedding_model_kwargs={
            "api_base": "http://localhost:8080/v1",  # 로컬 임베딩 서버
        },
        embedding_dim=1024,
        max_token_size=8192,

        # 그래프 설정
        graph_storage="NetworkXStorage",      # 초기: NetworkX (경량)
        # graph_storage="Neo4JStorage",       # 프로덕션: Neo4j (확장성)

        # 청크 설정
        chunk_token_size=1200,
        chunk_overlap_token_size=100,

        # 엔티티 추출 설정
        entity_extract_max_gleaning=1,        # 추가 추출 라운드 (비용 vs 품질)
        entity_summary_to_max_tokens=500,

        # 한국어 최적화
        addon_params={
            "language": "Korean",             # 엔티티 추출 시 한국어 우선
            "entity_types": [                 # 상권 도메인 엔티티 타입
                "상권", "행정구역", "업종", "브랜드", "시설",
                "교통", "개발사업", "규제", "트렌드", "리스크"
            ]
        }
    )

    await rag.initialize_storages()
    await initialize_pipeline_status()
    return rag
```

### 3-3. 데이터 적재 전략 (Insert)

```python
# backend/data/lightrag_loader.py

async def load_district_knowledge(rag: LightRAG):
    """상권 지식 데이터를 LightRAG에 적재"""

    # ────────────────────────────────────────────
    # 1. 상권 프로필 텍스트 (정기 수집 데이터 → 텍스트 변환)
    # ────────────────────────────────────────────
    # PostgreSQL에서 상권 통계를 조회하여 자연어 텍스트로 변환 후 적재
    # LightRAG는 텍스트를 입력받아 자동으로 엔티티/관계를 추출

    district_text = """
    ## 강남역 상권 (2025년 4분기 기준)

    강남역 상권은 서울 강남구에 위치한 발달상권으로,
    일평균 유동인구는 약 85,000명이다.
    주요 유동인구는 20~30대 직장인으로 구성되어 있으며,
    점심시간(12~13시)과 퇴근시간(18~19시)에 피크를 보인다.

    상권 내 음식점은 총 320개이며, 카페가 89개로 가장 많다.
    최근 12개월 외식업 폐업률은 15.2%로 서울 평균(12.8%)보다 높다.
    월평균 카드매출은 약 180억원이며, 카페 업종이 32%를 차지한다.

    임대료는 1층 기준 평당 월 42만원으로 서울 내 최상위권이며,
    권리금은 업종에 따라 5,000만~2억원 수준이다.

    주요 리스크 요인:
    - 임대료 상승 압력 지속 (연 5~8% 인상)
    - 영동대로 지하공간 개발(2027 예정)로 상권 변동 가능성
    - 신논현 상권과의 경쟁 심화

    인접 상권: 신논현 상권, 역삼 상권, 강남대로 상권
    """
    await rag.ainsert(district_text)

    # ────────────────────────────────────────────
    # 2. 뉴스/트렌드 (주기적 크롤링 결과)
    # ────────────────────────────────────────────
    news_texts = [
        "2026년 1분기 서울 카페 시장은 포화 상태에 접어들었다. "
        "특히 강남역, 홍대입구 등 주요 상권에서 카페 폐업률이 "
        "전년 대비 20% 증가했다. 전문가들은 차별화된 메뉴와 "
        "경험 중심의 카페만 생존할 것으로 전망한다.",

        "정부는 2026년 소상공인 정책자금을 전년 대비 15% 확대한 "
        "4.2조원으로 편성했다. 특히 초기 창업자 대상 저금리 대출 "
        "한도가 1억원에서 1.5억원으로 상향되었다.",
    ]
    for text in news_texts:
        await rag.ainsert(text)

    # ────────────────────────────────────────────
    # 3. 법령/규제 텍스트
    # ────────────────────────────────────────────
    regulation_text = """
    ## 식품위생법 주요 조항 (음식점 창업 관련)

    제36조(시설기준): 식품접객업의 영업장에는 조리장, 객실을
    분리하여야 하며, 조리장에는 환기시설을 갖추어야 한다.

    제37조(영업허가 등): 식품접객업을 하려는 자는 관할 구청장에게
    영업허가를 받아야 한다. 일반음식점, 휴게음식점, 제과점 등으로
    구분되며, 주류 판매 시 별도 신고가 필요하다.

    주류판매 가능 조건:
    - 일반음식점 영업허가 보유
    - 학교 경계로부터 직선거리 200m 이상 (절대정화구역 외)
    - 청소년 출입 제한 조치 가능 시
    """
    await rag.ainsert(regulation_text)

    # ────────────────────────────────────────────
    # 4. 과거 분석 교훈 (TaskMemory와 별도로 Knowledge로도 적재)
    # ────────────────────────────────────────────
    lesson_text = """
    ## 상권분석 교훈 모음

    교훈 1: 강남역 이면도로 외식업 매출은 메인 도로 대비 40~60% 수준.
    매출 추정 시 반드시 도로 유형(대로변/이면도로)을 구분해야 한다.

    교훈 2: 재개발 지역 인근 상권의 유동인구 증가는 공사 인력 때문인
    경우가 많다. 공사 완료 후 유동인구가 급감할 수 있으므로
    재개발 일정을 반드시 확인해야 한다.

    교훈 3: 프랜차이즈 정보공개서의 평균 매출은 실제보다 20~30% 높게
    보고되는 경향이 있다. 하위 25% 점포 매출을 기준으로 보수적 추정 권장.
    """
    await rag.ainsert(lesson_text)
```

### 3-4. 에이전트의 LightRAG 검색 (Query)

```python
# backend/agents/specialists/population.py — 예시

async def analyze_population(rag: LightRAG, location: str, industry: str):
    """인구분석 에이전트가 LightRAG를 활용하는 방식"""

    # ────────────────────────────────────────────
    # 검색 모드별 활용
    # ────────────────────────────────────────────

    # 1. naive 모드 — 단순 벡터 유사도 검색 (가장 빠름)
    # 용도: 특정 상권의 기본 정보 빠르게 조회
    basic_info = await rag.aquery(
        f"{location} 상권 유동인구 특성",
        param=QueryParam(mode="naive")
    )

    # 2. local 모드 — 엔티티 중심 검색 (관계 1홉)
    # 용도: 특정 상권의 직접 연결된 정보 (인접 상권, 주요 시설 등)
    local_context = await rag.aquery(
        f"{location} 상권의 인접 경쟁 상권과 주요 집객 시설",
        param=QueryParam(mode="local")
    )

    # 3. global 모드 — 전체 그래프 탐색 (가장 포괄적)
    # 용도: 업종 전체 트렌드, 규제 동향 등 넓은 범위
    global_trend = await rag.aquery(
        f"{industry} 업종의 전국적 트렌드와 리스크 요인",
        param=QueryParam(mode="global")
    )

    # 4. hybrid 모드 — local + global 결합 (가장 권장)
    # 용도: 특정 상권 + 업종의 종합적 컨텍스트
    comprehensive = await rag.aquery(
        f"{location}에서 {industry} 창업 시 고려해야 할 "
        f"인구 특성, 트렌드, 리스크 요인",
        param=QueryParam(mode="hybrid")
    )

    return {
        "basic_info": basic_info,
        "local_context": local_context,
        "global_trend": global_trend,
        "comprehensive": comprehensive,
    }
```

### 3-5. LightRAG Knowledge Graph 구조 설계

```
상권분석 도메인의 엔티티-관계 모델:

[엔티티 타입]
─────────────
상권:       강남역 상권, 홍대입구 상권, 합정동 상권 ...
행정구역:    강남구, 마포구, 서초구 ...
업종:       카페, 한식, 퓨전한식, 미용, 디저트 ...
브랜드:     스타벅스, 이디야, 백다방 ...
시설:       강남역 2호선, 홍대입구역, CGV 강남, 강남세브란스 ...
개발사업:    영동대로 지하공간 개발, 합정 재개발 ...
규제:       식품위생법 제36조, 강남구 옥외광고물 조례 ...
트렌드:     건강식 트렌드, 1인 가구 증가, 배달 시장 성장 ...
리스크:     임대료 상승, 젠트리피케이션, 인구 감소 ...

[관계 타입]
─────────────
상권 ─── located_in ──→ 행정구역
상권 ─── has_dominant_industry ──→ 업종
상권 ─── neighboring ──→ 상권
상권 ─── competing_with ──→ 상권
상권 ─── has_risk ──→ 리스크
상권 ─── affected_by ──→ 개발사업
상권 ─── near_facility ──→ 시설
업종 ─── regulated_by ──→ 규제
업종 ─── influenced_by ──→ 트렌드
브랜드 ─── operates_in ──→ 상권
브랜드 ─── belongs_to ──→ 업종
개발사업 ─── impacts ──→ 상권
트렌드 ─── creates_risk ──→ 리스크
트렌드 ─── creates_opportunity ──→ 업종
```

### 3-6. LightRAG 운영 고려사항

```
1. 데이터 갱신 전략:
   ─────────────────
   - 상권 프로필: 분기 1회 전체 재적재 (delete + insert)
   - 뉴스/트렌드: 주 1회 incremental insert
   - 법령: 변경 시에만 업데이트
   - 분석 교훈: 분석 완료 후 즉시 insert

2. 스토리지 선택:
   ─────────────────
   - Phase 1 (MVP): NetworkXStorage (파일 기반, 단일 인스턴스)
   - Phase 2+: Neo4jStorage (다중 인스턴스, 확장성)
   - 벡터 스토어: ChromaDB (ReMe와 통일)

3. 성능 최적화:
   ─────────────────
   - 검색 결과 캐싱: 동일 상권+업종 쿼리는 Redis에 24h 캐시
   - 청크 사이즈: 1200 토큰 (상권 데이터 특성상 문단 단위가 적절)
   - 임베딩 배치: 대량 적재 시 batch insert로 임베딩 호출 최소화

4. 한국어 특수 고려:
   ─────────────────
   - bge-m3 임베딩 모델 사용 (한/영 다국어 지원, 1024dim)
   - 엔티티 추출 시 한국어 고유명사(상권명, 행정동명) 인식률 확인 필요
   - LLM의 엔티티 추출 프롬프트에 "Korean" 언어 설정 필수
```

---

## 4. 프레임워크 및 툴 정리

### 4-1. 핵심 프레임워크 — 역할별 정리

```
┌─────────────────────────────────────────────────────────────────┐
│                    프레임워크 아키텍처 맵                          │
│                                                                 │
│  ┌─────────────┐                                                │
│  │  LangGraph  │ ← 에이전트 오케스트레이션의 중심               │
│  │             │   DAG 워크플로우, 상태 관리, 체크포인팅           │
│  └──────┬──────┘                                                │
│         │ 에이전트 노드에서 LLM 호출                              │
│         ↓                                                       │
│  ┌─────────────┐                                                │
│  │  LiteLLM    │ ← 멀티 LLM 라우팅 (Claude, Gemini, GPT)       │
│  │             │   단일 인터페이스로 모델 스위칭                   │
│  └──────┬──────┘                                                │
│         │ 에이전트가 도구 호출 시                                  │
│         ↓                                                       │
│  ┌─────────────┐                                                │
│  │  MCP SDK    │ ← 외부 데이터 접근 표준 프로토콜                 │
│  │  (Python)   │   도구 자동 발견, API 키 관리, 캐싱              │
│  └──────┬──────┘                                                │
│         │ 검색 증강                                               │
│         ↓                                                       │
│  ┌─────────────┐  ┌───────────┐  ┌───────────┐                 │
│  │  LightRAG   │  │  ChromaDB │  │   ReMe    │                 │
│  │ (KG + RAG)  │  │ (벡터DB)  │  │ (메모리)  │                 │
│  └─────────────┘  └───────────┘  └───────────┘                 │
└─────────────────────────────────────────────────────────────────┘
```

### 4-2. 프레임워크 상세 비교 & 선택 이유

#### 오케스트레이션: LangGraph vs CrewAI vs AutoGen

| 항목 | LangGraph | CrewAI | AutoGen |
|------|-----------|--------|---------|
| **아키텍처** | DAG 기반 그래프 | 역할 기반 에이전트 팀 | 대화 기반 멀티에이전트 |
| **상태 관리** | TypedDict State (강력) | 내부 관리 (제한적) | 메시지 기반 |
| **병렬 실행** | 네이티브 지원 (fan-out) | 제한적 | 제한적 |
| **체크포인팅** | 내장 (SQLite/PostgreSQL) | 없음 | 없음 |
| **조건 분기** | 유연 (conditional_edge) | 제한적 | 코드 기반 |
| **커스텀 도구** | 완전 지원 | 지원 | 지원 |
| **LLM 독립성** | LiteLLM 통합 용이 | LangChain 의존 | OpenAI 중심 |
| **학습 곡선** | 중간~높음 | 낮음 | 중간 |
| **프로덕션 성숙도** | 높음 (LangChain 팀) | 중간 | 중간 |

**선택: LangGraph**
- 이유: 에이전트 간 의존성(매출→재무), 병렬 실행(인구∥경쟁∥트렌드), 조건 분기(Debate 트리거)를 DAG로 명확하게 표현 가능
- 기존 side 프로젝트에서 LangGraph 사용 경험 활용

#### LLM 라우팅: LiteLLM vs LangChain Chat Models

| 항목 | LiteLLM | LangChain Chat Models |
|------|---------|----------------------|
| **지원 모델** | 100+ (거의 모든 LLM) | 주요 모델만 |
| **통합 인터페이스** | OpenAI 호환 단일 API | 모델별 래퍼 클래스 |
| **프록시 서버** | 내장 (독립 서비스) | 없음 |
| **비용 추적** | 내장 콜백 | Langfuse 연동 필요 |
| **폴백/로드밸런싱** | 내장 | 직접 구현 |
| **스트리밍** | 지원 | 지원 |

**선택: LiteLLM**
- 이유: Commander(Opus), Specialist(Gemini Flash), Critic(Sonnet) 등 모델이 다양하므로 단일 인터페이스가 필수
- LiteLLM 프록시를 Docker 컨테이너로 띄우면 API 키 관리도 중앙화

#### Knowledge Graph: LightRAG vs GraphRAG(MS) vs LlamaIndex KG

| 항목 | LightRAG | GraphRAG (MS) | LlamaIndex KG |
|------|----------|---------------|---------------|
| **그래프+벡터 통합** | 네이티브 | 그래프 중심 | 벡터 중심 |
| **검색 모드** | naive/local/global/hybrid | local/global | vector/kg |
| **설치 복잡도** | pip install 1줄 | 복잡 (Neo4j 필수) | 중간 |
| **LLM 비용** | 낮음 (경량 추출) | 높음 (커뮤니티 감지) | 중간 |
| **한국어** | bge-m3 설정 가능 | 제한적 | 설정 가능 |
| **증분 업데이트** | 지원 | 전체 재빌드 | 지원 |
| **GitHub Stars** | 28K+ | 24K+ | 38K+ |

**선택: LightRAG**
- 이유: 기존 side 프로젝트에서 사용 중, 가볍고 빠른 설치, hybrid 검색 모드가 상권분석에 적합
- 상권 데이터는 규모가 크지 않아 (전국 ~3,000개 상권) NetworkX로도 충분

### 4-3. 전체 기술 스택 — 최종 정리

```
┌─────────────────────────────────────────────────────────────────┐
│  카테고리         │ 기술                │ 버전      │ 용도         │
├──────────────────┼────────────────────┼──────────┼─────────────┤
│                  │                    │          │             │
│  [오케스트레이션]  │ LangGraph          │ ≥0.3     │ 에이전트 DAG │
│                  │ LiteLLM            │ ≥1.60    │ LLM 라우팅   │
│                  │ Langfuse           │ ≥3.0     │ LLM 모니터링 │
│                  │                    │          │             │
│  [LLM]           │ Claude Opus 4.6    │ -        │ Commander   │
│                  │ Claude Sonnet 4.6  │ -        │ Critic 등   │
│                  │ Gemini 2.5 Flash   │ -        │ Specialist  │
│                  │ Gemini 2.5 Pro     │ -        │ Advocate    │
│                  │                    │          │             │
│  [RAG/메모리]     │ LightRAG           │ ≥1.4     │ Knowledge   │
│                  │ ChromaDB           │ ≥1.3     │ 벡터 스토어  │
│                  │ ReMe               │ -        │ 에이전트메모리│
│                  │ bge-m3             │ -        │ 임베딩 모델  │
│                  │                    │          │             │
│  [MCP]           │ mcp-python-sdk     │ ≥1.0     │ MCP 서버 구축│
│                  │                    │          │             │
│  [백엔드]         │ FastAPI            │ ≥0.115   │ API 서버    │
│                  │ Pydantic           │ ≥2.0     │ 스키마 검증  │
│                  │ httpx              │ ≥0.27    │ 비동기 HTTP  │
│                  │ APScheduler        │ ≥3.10    │ 데이터 수집  │
│                  │ Celery             │ ≥5.4     │ 비동기 태스크│
│                  │                    │          │             │
│  [데이터베이스]    │ PostgreSQL+PostGIS │ ≥16      │ 정형+공간   │
│                  │ Redis              │ ≥7.0     │ 캐시, 큐    │
│                  │                    │          │             │
│  [프론트엔드]     │ Next.js            │ ≥15      │ 웹 앱       │
│                  │ shadcn/ui          │ -        │ UI 컴포넌트  │
│                  │ Tailwind CSS       │ ≥4.0     │ 스타일링    │
│                  │ Plotly.js          │ ≥2.35    │ 차트        │
│                  │ Mapbox GL JS       │ ≥3.0     │ 지도        │
│                  │ Zustand            │ ≥5.0     │ 상태 관리   │
│                  │                    │          │             │
│  [인프라]         │ Docker Compose     │ ≥2.0     │ 로컬 개발   │
│                  │ GCP Cloud Run      │ -        │ 프로덕션    │
│                  │ GCP Cloud SQL      │ -        │ 관리형 DB   │
│                  │ GitHub Actions     │ -        │ CI/CD       │
│                  │                    │          │             │
│  [데이터 수집]    │ PublicDataReader   │ ≥2025    │ 공공API래퍼 │
│                  │ (WooilJeong/PDR)   │          │             │
│                  │ Playwright         │ ≥1.45    │ 웹 크롤링   │
└──────────────────┴────────────────────┴──────────┴─────────────┘
```

### 4-4. 유용한 Python 라이브러리 추가 추천

| 라이브러리 | 용도 | 왜 필요한가 |
|-----------|------|------------|
| **PublicDataReader** | data.go.kr API 래퍼 | 국토부 실거래가, 소진공 상가정보를 pandas DataFrame으로 바로 받아옴. 직접 API 파싱할 필요 없음 |
| **geopandas** | 공간 데이터 처리 | 상권 경계 폴리곤 연산, 반경 내 POI 필터링 |
| **shapely** | 기하학 연산 | 좌표 간 거리 계산, 상권 영역 포함 여부 판단 |
| **pyproj** | 좌표 변환 | TM좌표 ↔ WGS84 변환 (공공데이터 좌표계가 제각각) |
| **folium** | 지도 시각화 (서버사이드) | 분석 결과를 HTML 지도로 렌더링 → 프론트엔드로 전달 |
| **pytrends** | Google Trends | 네이버 데이터랩 보완용 글로벌 트렌드 |

### 4-5. Docker Compose 서비스 구성 (예상)

```yaml
# docker-compose.yml

services:
  # ── 핵심 서비스 ──
  backend:
    build: ./backend
    ports: ["8000:8000"]
    depends_on: [postgres, redis, reme, litellm]
    environment:
      - DATABASE_URL=postgresql://...
      - REDIS_URL=redis://redis:6379
      - LITELLM_URL=http://litellm:4000
      - REME_URL=http://reme:8002
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}
      - GEMINI_API_KEY=${GEMINI_API_KEY}
      - DATA_GO_KR_API_KEY=${DATA_GO_KR_API_KEY}
      - KAKAO_REST_API_KEY=${KAKAO_REST_API_KEY}
      - NAVER_CLIENT_ID=${NAVER_CLIENT_ID}
      - NAVER_CLIENT_SECRET=${NAVER_CLIENT_SECRET}

  frontend:
    build: ./frontend
    ports: ["3000:3000"]
    depends_on: [backend]

  # ── 데이터 ──
  postgres:
    image: postgis/postgis:16-3.4
    ports: ["5432:5432"]
    volumes: [pgdata:/var/lib/postgresql/data]

  redis:
    image: redis:7-alpine
    ports: ["6379:6379"]

  chromadb:
    image: chromadb/chroma:latest
    ports: ["8100:8000"]
    volumes: [chromadata:/chroma/chroma]

  # ── AI 서비스 ──
  litellm:
    image: ghcr.io/berriai/litellm:main-latest
    ports: ["4000:4000"]
    volumes: [./litellm_config.yaml:/app/config.yaml]
    command: ["--config", "/app/config.yaml"]

  reme:
    build: ./reme
    ports: ["8002:8002"]

  embedding:
    image: ghcr.io/huggingface/text-embeddings-inference:latest
    ports: ["8080:80"]
    command: ["--model-id", "BAAI/bge-m3"]
    deploy:
      resources:
        reservations:
          devices:
            - capabilities: [gpu]  # GPU 있으면 활용

  # ── 모니터링 ──
  langfuse:
    image: langfuse/langfuse:latest
    ports: ["3001:3000"]
    depends_on: [postgres]

volumes:
  pgdata:
  chromadata:
```

---

## 5. 기존 계획서 수정 권고 사항

위 검토를 통해 발견된 **기존 계획서의 수정이 필요한 부분:**

| # | 기존 계획서 내용 | 문제 | 권고 |
|---|-----------------|------|------|
| 1 | 인스타그램 해시태그 MCP | API 공식 폐쇄됨 | 네이버 블로그/카페 검색 + Google Trends로 대체 |
| 2 | 로드뷰 기반 가시성 분석 | 로드뷰 이미지 API 미제공 | MVP에서는 rule-based 가시성 점수, Vision LLM은 Phase 3+ |
| 3 | 권리금 데이터 → 부동산 MCP | 공공 API 없음 | 자체 추정 모델 구축 필요 (별도 태스크) |
| 4 | 객단가 데이터 누락 | 매출분석에 필수인데 소스 미기재 | 프랜차이즈 정보공개서 기반 자체 DB 구축 |
| 5 | 임베딩 모델 bge-m3 | 기존 side의 bge-small-en과 불일치 | 상권분석은 한국어 중심이므로 bge-m3가 적합, 별도 인스턴스 |
| 6 | 서울 외 지역 데이터 | 서울 열린데이터 중심 설계 | TAGO, 통계청 등 전국 데이터 API 보충 필요 |
| 7 | PublicDataReader 미언급 | API 파싱을 직접 구현하려 함 | PDR 라이브러리로 개발 시간 대폭 절감 가능 |
| 8 | 배달 매출 비중 미반영 | 외식업 매출의 30~50% | LightRAG에 배달 트렌드 리포트 적재, 업종별 배달 비중 테이블 자체 구축 |

---

> **다음 액션:** 이 보강 문서를 기존 계획서에 통합할지, 별도 레퍼런스로 유지할지 결정 후 Phase 0 (API 키 발급 + Docker 세팅)부터 시작
