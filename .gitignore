__pycache__/
*.pyc
*.pyo
.pytest_cache/
.ruff_cache/
*.egg-info/
dist/
build/
.venv/
.venv-test/

# Environment secrets
# .env 는 prod 배포용 (git 커밋 금지). .env.dev 는 로컬 개발용.
.env
.env.dev
.env.local
.env.backup
.env.e2e
!.env.example

# Node
node_modules/
.next/

# Docker
pgdata/
# Dev-only compose override (volume-mount + --reload). 절대 프로덕션 배포 금지.
docker-compose.override.yml

# IDE
.idea/
.vscode/
*.swp

# Playwright MCP
.playwright-mcp/

# Test results
frontend/test-results/
frontend/e2e/artifacts/

# Screenshots (temp)
map-loaded.png

# Data files (downloaded separately, large binaries)
data/shp/
data/csv/

# Load test results
loadtest/results/

# Seed dump IS tracked via Git LFS
!data/seed/
server/server-debug.log
test-results/

# Claude Code local settings (personal permissions; team-shared config lives in settings.json)
.claude/settings.local.json
.claude/scheduled_tasks.lock
CLAUDE.local.md
.gstack/
data/backups/

# 자동배포 run 로그 + 상태 파일 (scripts/deploy/auto_deploy.sh)
data/deploy-logs/
# 빌드 중 auto_deploy 가 임시 이동한 .env.dev (비정상 종료 잔존물)
.env.dev.autodeploy-moved
